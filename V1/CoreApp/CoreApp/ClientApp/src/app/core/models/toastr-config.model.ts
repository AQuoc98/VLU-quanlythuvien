export interface ToastrConfig {
    toastTimeout: number,
    dismiss: string,
    newestOnTop: boolean,
    showCloseButton: boolean,
    maxShown: number,
    position: string,
    messageClass: string,
    titleClass: string,
    animate: string,
    enableHTML: boolean
}